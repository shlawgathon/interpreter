{
  "name": "@interpreter/server",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@interpreter/shared": "workspace:*",
    "@speechmatics/real-time-client": "^8",
    "@speechmatics/auth": "^0.1",
    "convex": "^1"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7"
  }
}
