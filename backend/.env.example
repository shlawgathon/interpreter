# ── Interpreter Backend Environment Variables ──

# Speechmatics Real-time STT API Key
# Get from: https://portal.speechmatics.com
# Use code VOICEAGENT200 for $200 credit
SPEECHMATICS_API_KEY=

# MiniMax API key
# Get from: https://www.minimax.io
MINIMAX_API_KEY=

# Optional model overrides
# Defaults:
# - MINIMAX_TEXT_MODEL=MiniMax-M2
# - MINIMAX_TTS_MODEL=speech-2.8-turbo
MINIMAX_TEXT_MODEL=
MINIMAX_TTS_MODEL=

# Translation provider:
# 1 = use Speechmatics realtime translation (lower latency, recommended)
# 0 = use MiniMax text translation path
USE_SPEECHMATICS_TRANSLATION=1

# TTS provider:
# - minimax (default): best multilingual coverage
# - speechmatics: preview TTS endpoint, currently English-focused (good for testing)
TTS_PROVIDER=minimax

# Translation chunking / latency tuning
# Higher TRANSLATION_TRIGGER_CHAR_THRESHOLD = fewer, larger translation requests (more stable text, more latency)
# Lower TRANSLATION_TRIGGER_CHAR_THRESHOLD = faster updates (more frequent chunking)
TRANSLATION_TRIGGER_CHAR_THRESHOLD=24
# Throttle streamed partial text updates to reduce "rewriting" flicker in UI
TRANSLATION_PARTIAL_MIN_DELTA_CHARS=12
TRANSLATION_PARTIAL_MIN_INTERVAL_MS=300
# Speechmatics finalization delay (seconds). Lower = faster finals, possibly less stable segmentation.
SPEECHMATICS_MAX_DELAY=1.0
# Speechmatics realtime transcription websocket URL (must be ws:// or wss://).
SPEECHMATICS_RT_WS_URL=wss://eu.rt.speechmatics.com/v2/
# Emit Speechmatics translation partials.
SPEECHMATICS_TRANSLATION_ENABLE_PARTIALS=1

# Speechmatics TTS options (used when TTS_PROVIDER=speechmatics)
SPEECHMATICS_TTS_OUTPUT_FORMAT=wav_16000
SPEECHMATICS_TTS_VOICE_ID=
# SPEECHMATICS_TTS_VOICE_ID_EN=sarah

# Optional TTS voice overrides:
# - MINIMAX_TTS_VOICE_ID (global override, e.g. English_expressive_narrator)
# - MINIMAX_TTS_VOICE_ID_ES (per-language override, e.g. Spanish_SereneWoman)
MINIMAX_TTS_VOICE_ID=
# MINIMAX_TTS_VOICE_ID_EN=
# MINIMAX_TTS_VOICE_ID_ES=

# Legacy field (kept for backward compatibility; not required by current endpoints)
MINIMAX_GROUP_ID=
